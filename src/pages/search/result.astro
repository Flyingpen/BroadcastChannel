---
export const prerender = true
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <title>正在跳转…</title>
    <meta name="robots" content="noindex, nofollow" />
    <meta http-equiv="refresh" content="0;url=/" />
    <style>
      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family:
          system-ui,
          -apple-system,
          BlinkMacSystemFont,
          'Segoe UI',
          sans-serif;
        background: #f5f5f5;
        color: #444;
      }
      a {
        color: inherit;
      }
    </style>
  </head>
  <body>
    <p>
      正在跳转，请稍候…如长时间未跳转，<a id="fallback" href="/">点击返回首页</a>
    </p>
    <script>
      const params = new URLSearchParams(window.location.search)
      const keyword = params.get('q')?.trim()
      const target = keyword ? `/search/${encodeURIComponent(keyword)}` : '/'
      const link = document.getElementById('fallback')
      link.href = target
      window.location.replace(target)
    </script>
  </body>
</html>
